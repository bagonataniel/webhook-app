services:
  - name: my-static-site
    github:
      repo: bagonataniel/webhook-app
      branch: main
    type: static
    buildCommand: cd app/public && npm install && npm run build
    staticPublishPath: app/public
